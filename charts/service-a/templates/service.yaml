apiVersion: v1
kind: Service
metadata:
  name: service-a
spec:
  type: {{ .Values.service.type }}
  selector:
    app: service-a
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
